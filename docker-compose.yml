services:
  dev-db:
    image: postgres:latest
    ports:
      - '5234:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pg123
      POSTGRES_DB: nest
    networks:
      - sufi
networks:
  sufi:
